#pragma once
#include <iostream>
#include <ctime>
#include <chrono>
#include <string>
#include <vector>
using namespace std;
void Menu();
void QuickSort(pair<string, size_t>* students, size_t beg, size_t end);